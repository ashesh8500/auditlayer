# AuditLayer Improvement Ledger

Append-only record of autonomous improvement verdicts on branch `improve/alm-recursive-2026-08-07`.

| ID | Programs / stories | Idea | Verdict | Check | Evidence / finding |
|---|---|---|---|---|---|
| ALM-I-000 | P1-P8 / C1-C10, F1-F6, D1-D5 | Establish the autonomous improvement constitution | verified | Vision, fundamentals, stories, queue, worker profile, board, and scheduled assigner exist and pass configuration checks | Bootstrap artifact; no product-quality or customer-outcome claim. |
| ALM-I-001 | P1 / C1/C2 / D3 | Enforce evidence coverage for the six customer answers | verified | `cd worker && uv run pytest tests/test_intelligence_evidence_coverage.py tests/test_intelligence_contracts.py -q` → 21 passed (exit 0); full worker intelligence suite 129 passed | Coverage contract (`worker/auditlayer_worker/intelligence/coverage.py`) + 8-fixture corpus: every material claim must resolve to evidence with observed_at/expires_at/confidence/limitations; unknown/missing/stale/fabricated-precision fail closed; unavailable answers render only `Data needed`. Fixtures prove the software contract, not calibration or efficacy. |
| ALM-I-002 | P5 / C7/C8 / D4 | Make customer waiting-state projection progress-aware | verified | `cd web && pnpm exec vitest run src/lib/intelligence/client-status.test.ts src/lib/intelligence/user-stories.test.ts` → 47 passed (exit 0); `pnpm exec tsc --noEmit` exit 0; full `src/lib/intelligence` suite 74 passed | Staleness measured from newest meaningful progress event (not heartbeat/cache/retry noise); terminal always wins; deterministic clock injection; serializeStatus/hydrateStatus for leave-and-return resume. Fixtures prove projection contracts only. |
| ALM-I-003 | P8 / C9 / D5 | Define the recommendation-outcome linkage contract | verified | `python3 scripts/check-migrations.py` → exit 0, latest=20260807000000; `python3 supabase/tests/recommendation_outcomes_contract_test.py` → RECOMMENDATION OUTCOME CONTRACT PASSED (29 assertions) | Additive migration 20260807000000_recommendation_outcome_ledger.sql (NOT NULL FKs to recommendations+subjects, decision_state, ordered window, outcome_data, confounding_notes, evidence_ids; RLS select-own + admin; service-role-only SECURITY DEFINER write RPC with fixed search_path; anon revoked). Static contract verifies schema/security invariants only; no live SQL or efficacy claim. |
| ALM-I-004 | P2 / C5 / F5 / D3 | Classify audit deltas honestly | verified | `cd worker && uv run pytest tests/test_intelligence_change_classifier.py tests/test_intelligence_runtime.py tests/test_intelligence_ledger_writer.py -q` → 60 passed (exit 0); full intelligence suite 167 passed (rerun at gate) | Deterministic change classifier (`worker/auditlayer_worker/intelligence/change_classifier.py`) + 14 fixtures: every delta classified as evidence/brief_lens/methodology/prior_correction only when supported; missing/contradictory/multiple-change metadata yields honest UNKNOWN with a correction tip (never silent evidence); UNKNOWN projects to NULL change_kind at the ledger boundary with kernel CHECK untouched; no supabase/SQL changes. Fixtures prove the software contract only. |
| ALM-I-005 | P11 / C14 / F7/F8 / D2/D8 | Make offer promises contract-backed | verified | `cd web && corepack pnpm exec vitest run src/lib/offer-contract.test.ts src/lib/intelligence/batch.test.ts` → 51 passed (39 + 12, exit 0); `corepack pnpm exec tsc --noEmit` → exit 0; full `src/lib` suite 156 passed | Versioned typed web offer contract (`web/src/lib/offer-contract.ts` v1.0.0) + dependency-free price table (`offer-pricing.ts`) + 39 parity/fail-closed tests. Landing pricing consumes the contract; prices ($30/$50), audit limits (1/5/15/10000), report types, and trial overlays agree with domain.ts, stripe.ts, and service-role entitlement SQL (static inspection); unsupported promises (refinements, six-dimension score, white-label/API) are typed UNKNOWN and no longer rendered as delivered value. Fixture-backed software contract; no efficacy/WTP/funnel claim. |
| ALM-I-006 | P12 / C15 / F9 / D7 | Fail closed on auth and capability gaps | verified | `python3 scripts/tests/test_check_alm_capabilities.py` → 138 assertions (exit 0); complete fixture → exit 0, 8/8 ready, 0 blocked, external 8 unknown, secret_values_emitted=false; missing fixture → exit 2 blocking exactly preview_login/instagram_oauth/worker_commands/deepseek_policy with recovery tips; production fixture → preview_login blocked by VERCEL_ENV policy; `python3 scripts/check-migrations.py` → exit 0, 42 files | Deterministic no-secret capability preflight (`scripts/check_alm_capabilities.py`) + 3 fixtures + static contract suite. Presence-only env checks; external/live checks always UNKNOWN with release-gate commands; redaction assertions confirm no secret values or observed policy values ever emitted. Fixtures prove classification and redaction only, never live provider availability or login success. |
| ALM-I-007 | P3 / C1/C6 / F1 / D2/D3/D8 | Enforce honest-null report projection quality | verified | `cd worker && uv run pytest tests/test_report_quality_contract.py tests/test_intelligence_evidence_coverage.py tests/test_pipeline_finalization.py -q` → 41 passed (exit 0) in parent and gate worktrees; full worker suite 409 passed / 12 skipped (exit 0); `make alm-check` exit 0 (167 worker + 74 web tests) | Canonical final quality gate (`worker/auditlayer_worker/quality.py`) fails closed on 15/15 canonical section order, 6/6 six-answer coverage, strengths, ranked priorities, honest-null `Data needed`, safe static HTML, and print CSS for standard reports; PROMPT_VERSION 1.1→1.2 with print CSS added to master-skeleton.html; deterministic golden `standard_report` fixture derived from the W001 typed corpus; 22-test fail-closed regression matrix. Honest-null/fabricated-precision enforcement is a conservative unit-pattern heuristic scoped to data_needed mapped sections; typed answer_states remain authoritative at the pipeline call site. Fixtures prove the software contract, not creator efficacy or calibration. |
| ALM-I-008 | P7 / C10/C13 / F2 / D2/D3/D8 | Property-test tenant and artifact access boundaries | verified | `cd web && corepack pnpm exec vitest run src/lib/access-boundary.test.ts` → 85 passed (exit 0) in parent and gate worktrees; `python3 supabase/tests/tenant_access_contract_test.py` → TENANT ACCESS CONTRACT PASSED (exit 0); `cd web && corepack pnpm exec tsc --noEmit` → exit 0; `make alm-check` exit 0 | Canonical deterministic access-decision contract (`web/src/lib/access-boundary.ts`) consumed by audit-access.ts, share-access.ts, and all three private report/read/share routes; 85-test fixture-generated principal×resource matrix (48 cases, 18 allow / 30 deny) plus share-state, ordering, version-scoping, cookie-reachability, and redaction tests; static RLS/storage contract checker inspects 42 migrations and 24 policies; email-share session cookie path fixed to token-scoped `/`. RLS/storage remain authoritative; the matrix centralizes only server-side decision projection. Live RLS behavior under real JWTs is not statically provable and is marked UNKNOWN with an exact local probe (release gate only). |
| ALM-I-009 | P5 / C4/C9 / F4 / D3/D8 | Enforce Living Brief protected proposal semantics | verified | `python3 scripts/check-migrations.py` → exit 0, 43 files latest=20260807120000; `python3 supabase/tests/living_brief_proposal_contract_test.py` → LIVING BRIEF PROPOSAL CONTRACT PASSED (40 assertions) in parent and gate worktrees; `cd worker && uv run pytest tests/test_intelligence_adversarial.py tests/test_intelligence_continuity_ledger.py -q` → 83 passed (parent and gate worktrees); `make alm-check` exit 0 (ontology OK; 192 worker + 74 web tests) | Worker proposal validator extended with a Living Brief path policy: 7 proposable fields, 4 protected (identity, positioning, goals, constraints) requiring explicit owner confirmation, 3 unprotected (audience, offers, experiments); bounded RFC 6901 pointer validation; evidence-linked semantic fingerprints; unchanged-evidence recurrence suppression at validator and runtime assembly. Additive migration 20260807120000_living_brief_proposal_semantics.sql: rejected_context_proposals ledger with unique (subject_id, semantic_fingerprint) recurrence gate, atomic owner-confirmed resolve RPC (accept appends confirmed next version + durable decision; reject records decision + fingerprint without a version), create-RPC recurrence rejection, SECURITY DEFINER with fixed search_path, browser roles SELECT-only. 40-assertion static contract test + 13 new worker cases; 9×3 accept/reject/stale-recurrence matrix fails closed on wrong owner, stale base, duplicate resolution, and protected-path-without-confirmation. Supabase remains canonical for Living Brief state. Static contract verifies schema/security invariants only; no live SQL or efficacy claim. |
| ALM-I-010 | P6 / F4 / D3/D8 | Quantify bounded runtime and deadline invariants | verified | `cd worker && uv run pytest tests/test_intelligence_runtime_budget_contract.py tests/test_intelligence_runtime.py tests/test_intelligence_contracts.py -q` → 37 passed (parent and gate worktrees); `cd worker && uv run pytest tests/test_intelligence_adversarial.py -q` → 64 passed (parent); `make alm-check` exit 0 (ontology OK; 192 worker + 74 web tests); runtime fixture records provider_calls=0 | Canonical `BoundedIntelligenceRuntime` extended with a deterministic total run deadline: RunDeadlineExceeded distinct from per-call inference_timeout, telemetry deadline_seconds/deadline_exceeded (allowlist only), worker-batched channel submission so queued stage work cannot start after the deadline, completed-stage preservation + pending cancellation, checks before projection/channel/synthesis. 12-case mock-only budget-contract harness emits a machine-readable evidence fixture (`worker/tests/fixtures/intelligence/runtime_budget/runtime_budget_evidence.json`) covering call/concurrency/synthesis/cache/resume/deadline/token/cost/stage counts with provider_calls=0. In-flight provider-call cancellation is the documented remaining boundary (per-call policy timeout still bounds each call). Fixtures prove the software contract, not live provider behavior or cost efficacy. |
| ALM-I-011 | P9 / C11/C13 / F8 / D2/D3/D8 | Bound and drift-test the semantic contract | verified | `python3 docs/implementation/alm-intelligence-v1/ontology/validate_ontology.py` → exit 0, ALL SEMANTIC CONTRACT CHECKS PASSED core_classes=18 competency_queries=6 invalid_fixtures=6 sql_drift=0 (parent and gate worktrees); `python3 -m json.tool docs/implementation/alm-intelligence-v1/ontology/semantic-contract-report.json >/dev/null` → exit 0; `make alm-check` exit 0 (ontology OK; 192 worker + 74 web tests) | CI-only ontology projection evolved in place (`docs/implementation/alm-intelligence-v1/ontology/`): explicit bounded core vocabulary (18 core / 14 non-core, declared=32, version 1.1), 13 SHACL invariants including new observed-target, decision-backed recommendation, and immutable-projection shapes, 6 competency queries with SQL twins (sql_drift=0), 6 named invalid fixtures each rejected by its intended invariant, deterministic `semantic-contract-report.json` (byte-identical across reruns; no env paths/timestamps). Ontology remains CI-only; Postgres and application contracts stay authoritative; no graph runtime or SQL authority changed. Fixtures prove the software contract, not calibration or efficacy. |
| ALM-I-012 | P10 / C12 / F1/F8 / D2/D8 | Enforce shared product-experience primitives | verified | `cd web && corepack pnpm exec vitest run src/lib/experience-contract.test.ts src/components/ui/experience-state.test.tsx` → 25 passed (exit 0, parent and gate worktrees); `cd web && corepack pnpm exec tsc --noEmit` → exit 0; `python3 -m json.tool web/artifacts/experience-contract.json >/dev/null` → exit 0; `make alm-check` exit 0 (ontology OK; 192 worker + 74 web tests) | Versioned experience contract v1.0.0 (`web/src/lib/experience-contract.ts`) covers all 28 portal routes with 10 rules and 61 live registry exceptions (0 stale, 0 violations); six raw panel surfaces migrated to the canonical alm-panel primitive and three banner surfaces to ExperienceBanner with semantic tones; deterministic conformance report (`web/artifacts/experience-contract.json`) regenerated from the contract source. Existing portal design tokens/primitives remain canonical. 44px touch-target and production accessibility remain UNKNOWN pending the exact Playwright+axe probe (release gate only). Fixtures prove the software contract, not subjective perfection. |
| ALM-I-013 | P7 / F3/F6 / F9 / D3/D8 | Build founder-facing release-evidence packet generation | verified | `python3 scripts/tests/test_build_alm_release_evidence.py` → ALM RELEASE EVIDENCE CONTRACT PASSED (215 assertions, exit 0, parent and gate worktrees); `python3 scripts/build_alm_release_evidence.py --output /tmp/alm-release-evidence.json` → exit 0; `python3 -m json.tool /tmp/alm-release-evidence.json >/dev/null` → exit 0; `python3 scripts/tests/test_check_alm_capabilities.py` → 138 assertions (exit 0, regression); `python3 scripts/check-migrations.py` → exit 0; `make alm-check` exit 0 (no live tokens) | Canonical non-mutating release-evidence packet generator (`scripts/build_alm_release_evidence.py`, schema v1) reads only local Git + the reused `scripts/check-migrations.py`/`scripts/check_alm_capabilities.py` contracts, projects observed state onto the operating-model promotion machine (integrated_local → preview_candidate → preview_verified → release_ready → production_canary → promoted | rolled_back | held), and treats absent/stale/incompatible-commit/failing evidence as UNKNOWN/BLOCKED with exact correction commands — never fabricated success; cannot deploy, migrate, approve, or mutate. 215-assertion contract test, 5 deterministic fixtures (`scripts/fixtures/alm-release-evidence/`), implementation-contract doc (`docs/improvements/RELEASE-EVIDENCE.md`); live packet names exact HEAD/origin/diff state and classifies all six external boundaries UNKNOWN/BLOCKED. Fixtures prove packet classification and redaction only, never a live preview, migration, canary, or promotion. |
| ALM-I-014 | P6/P12 · F4/F9 · D3/D4/D8 | Contain in-flight inference cancellation | verified | `cd worker && uv run pytest tests/test_intelligence_runtime_cancellation.py tests/test_intelligence_runtime_budget_contract.py tests/test_intelligence_runtime.py -q` → 38 passed (parent and gate worktrees); `cd worker && uv run pytest tests/test_intelligence_adversarial.py -q` → 72 passed (parent and gate worktrees); json.tool valid on budget + cancellation evidence fixtures with provider_calls=0; `make alm-check` exit 0 (ontology OK; 199 worker + 74 web tests) | Canonical `BoundedIntelligenceRuntime` total-deadline boundary for already-running channel inference: `wait(..., timeout=remaining_budget, return_when=FIRST_COMPLETED)` loop replaces `as_completed` so the runtime returns at the total deadline (0.501s vs the 150s per-call ceiling) without waiting out in-flight calls; cooperative transports receive a capped timeout `min(ceiling, remaining budget)` = 0.499s each; `_contain_deadline_batch` preserves completed stages, confirms queued cancellation (1), classifies in-flight termination as UNKNOWN (3) with a non-secret correction tip, and never persists late output (proven with a threading-event blocking fake: 0 late-persisted stages); resume keeps 4/4 stages cache_mode=resume. Allowlisted telemetry extended with queued_cancelled/inflight_unknown/cancellation_tip; inference.py documents the HTTPX `policy.timeout_seconds` adapter contract without behavior change. Deterministic mock-only blocking-fake harness (`worker/tests/fixtures/intelligence/runtime_cancellation/runtime_cancellation_evidence.json`, provider_calls=0). Unresolved transport uncertainty stays honest: in-process Python cannot prove a synchronous transport stopped, so in-flight termination is UNKNOWN, never labelled cancelled. Fixtures prove orchestration/containment, not live provider cancellation or latency. |
| ALM-I-015 | P6/P12 · F2/F4/F9 · D2/D3/D8 | Reconcile intelligence telemetry with authoritative generation-run persistence | verified | `cd worker && uv run pytest tests/test_intelligence_telemetry_persistence.py tests/test_intelligence_runtime_budget_contract.py tests/test_intelligence_bridge.py tests/test_intelligence_ledger_writer.py tests/test_claim_rpc.py -q` → 60 passed (parent and gate worktrees); `cd worker && uv run pytest tests/test_intelligence_adversarial.py -q` → 72 passed (parent and gate worktrees); json.tool valid on telemetry evidence fixture with provider_calls=0 (sha256 byte-identical across reruns); `python3 scripts/check-migrations.py` exit 0 (43 files, latest=20260807120000); `make alm-check` exit 0 (ontology OK; 224 worker + 74 web tests, no live tokens) | Canonical telemetry-to-persistence ownership/parity contract (`worker/auditlayer_worker/intelligence/telemetry_persistence.py`): each allowlisted `RuntimeTelemetry` field has exactly one authoritative owner — `intelligence_runs` subject-domain truth (status, cache_mode, tokens_in, tokens_out, cost_usd; latency_ms derived) vs private `report_generation_runs` attempt metrics (failure_code→error_code, evidence_items, report-stage stage_timings) — or an explicit UNSUPPORTED entry with correction tip (channel/synthesis/correction calls, model, provider, deadline fields, and W007's queued_cancelled/inflight_unknown/cancellation_tip with no persistence column today; stage_state/RPC authority out of scope). `finalize_payload`, the subject-ledger bridge, and the report adapter normalize through this single boundary with distinct status vocabularies (succeeded→completed vs succeeded→ready), honest nulls preserved, unknown cache_mode/non-allowlisted fields fail closed, and payload key sets disjoint (no double-write, no customer payload). Gate reconciled the W007 cancellation fields landed at origin before W008 as explicit UNSUPPORTED entries — one owner per field, parity with the 18-field runtime allowlist intact. 25 new fixture-backed tests + evidence fixture; fixtures prove mapping/parity/redaction only, never live persistence or latency. |
| ALM-I-016 | P7/P12 · C15 · F2/F9 · D2/D3/D8 | Project authoritative run health into a founder-only recovery view | verified | `cd web && corepack pnpm exec vitest run src/lib/admin-run-health.test.ts src/components/admin-run-health.test.tsx src/lib/admin-review.test.ts src/lib/access-boundary.test.ts src/components/ui/experience-state.test.tsx` → 153 passed (parent and gate worktrees); `cd web && corepack pnpm exec vitest run src` → 313 passed (25 files, parent and gate worktrees); `cd web && corepack pnpm exec tsc --noEmit` → exit 0; `python3 -m json.tool web/artifacts/admin-run-health-contract.json >/dev/null` → exit 0, provider_calls=0 (sha256 45973ac byte-identical across reruns); `make alm-check` exit 0 (ontology OK; 224 worker + 74 web tests, no live tokens) | Canonical deterministic founder run-health projection (`web/src/lib/admin-run-health.ts` v1.0.0 + server component `web/src/components/admin-run-health.tsx`) composed only inside the existing server-only `/admin` boundary behind `requireAdmin`. Consumes the two authoritative run records per the ALM-I-015 ownership contract with distinct, never-conflated status vocabularies (report-attempt running/ready/needs_review/failed/blocked/crashed vs intelligence running/completed/failed); injectable clock and deterministic age/ordering; `running` past the 10-minute threshold → `delayed`; impossible markers/clock reversal/future reference → `contradictory`; out-of-vocabulary status → `unknown`, never success. `total_deadline` and `cancellation` have no persistence column on either record and ALWAYS project UNKNOWN with exact correction tips — never fabricated success. Allowlisted health-field queries only (9 report + 7 intelligence fields); redaction-safe serialization structurally excludes handles, emails, evidence bodies, stage payload values, raw exceptions, credentials, and tracebacks; bounded error-code scrubbing (no control chars, ≤200 chars); no cross-record join (audit→batch→subject is many-to-many and not inferred); no new route, dashboard, schema, polling loop, service-role client import, second status system, or recovery mutation. 47 new fixture-backed tests (37 lib + 10 component); fixtures prove projection/access/redaction only, never live persistence or production health. |

## Verdict vocabulary

- `verified` — the predeclared check passed with inspectable evidence.
- `failed` — the claim was falsified; the result is useful and must remain visible.
- `partial` — a bounded software contract passed but the broader empirical claim remains unsupported.
- `parked` — the check cannot be identified or safely run with current evidence/capabilities.

A merge gate appends rows after verifying worker artifacts. Workers do not edit this file directly.
