# AuditLayer Improvement Idea Queue

Status vocabulary: `proposed → in_flight → done`; `parked` is explicit. The assigner changes `proposed` to `in_flight`; only a verified merge gate changes it to `done`.

| Status | Tag | Idea | Check | Type |
|---|---|---|---|---|
| done | P1 · C1/C2 · D3 | Build an evidence-coverage contract for the six customer answers | On a corpus of existing sanitized/mock report payloads, every material metric/finding/recommendation resolves to evidence ID + observed time + freshness + confidence/limitation; invalid payloads fail closed; no customer data or live calls | engineering |
| done | P2 · C5 · F5 | Produce a deterministic audit-to-audit change classifier | Fixture-backed versions classify every delta as evidence, brief/lens, methodology, or correction; unexplained deltas become UNKNOWN with a correction tip; backward-compatible serialization passes | engineering |
| in_flight | P3 · C6 · F1 | Measure recommendation specificity and actionability against the canonical method | A predeclared rubric applied to at least 20 sanitized repository report sections reports inter-rater-ready features and honest coverage; no efficacy claim from synthetic labels | research |
| in_flight | P4 · C3 · F1 | Add a same-tier peer validity auditor | Real repository peer fixtures and public test data reject tier mismatch, unverifiable handles, stale metrics, and relationship-framing errors; valid peers include rationale and source age | engineering |
| done | P5 · C7/C8 | Falsify customer waiting-state truthfulness under retries and resume | Deterministic state-machine tests cover preparing/analyzing/finalizing/delayed/success/failure, leave-return resume, stale heartbeat, and duplicate events; no internal trace leaks | engineering |
| done | P6 · F4 · D4 | Quantify bounded runtime reuse and retry invariants | Local mock runs prove single-channel one-call behavior, ≤3 independent channel calls, one synthesis, cache-key exactness, stage reuse, deadline enforcement, and zero live token spend | engineering |
| done | P7 · C10 · F2 · D3 | Property-test tenant and artifact access boundaries | Route/RLS contract tests cover owner/admin/other-user/anonymous matrices for briefs, reports, evidence, and signed access; no service-role secret reaches client output | security |
| done | P8 · C9 · D5 | Define the recommendation-outcome linkage contract | Typed schema and tests capture accepted/rejected/modified recommendation, experiment window, observed outcome, and confounding notes; unlinked outcomes cannot support efficacy claims | engineering |
| done | P3 · C1/C6 · F1 | Create an honest-null report quality regression harness | Golden structural fixtures verify six answers, limitation language, strengths, priorities, and print-safe HTML while explicitly avoiding subjective efficacy claims; diffs are inspectable | product |
| proposed | P4 · C3 · F1 | Audit niche and account-type misclassification paths | At least 30 sanitized/public test cases stratified by creator/company/expert/general-media and platform report accuracy, class balance, abstentions, and error categories; no recommendation when type is unknown | research |
| done | P5 · C4/C9 | Build Living Brief protected-field proposal semantics | Tests prove model output can only propose diffs; identity/vision/goals/constraints require explicit confirmation; version history remains immutable; rejected proposals stay rejected absent new evidence | engineering |
| done | P7 · F3/F6 | Build founder-facing release-evidence packet generation | One local command emits code/commit/test/preview/migration/production-state distinctions and rollback checklist from real local state; it cannot deploy or approve production | devops |

## Founder-brief expansion — 2026-08-07

| Status | Tag | Idea | Check | Type |
|---|---|---|---|---|
| done | P9 · C11/C13 · F8 | Surface a minimal brand-intelligence semantic contract | A versioned vocabulary with fewer than 25 core classes, SHACL/typed invariants, five competency queries, and valid/invalid fixtures answers partner/subject/evidence/decision/outcome questions without introducing a graph service or competing with SQL authority | architecture |
| proposed | P10 · C12 · F1/F8 | Establish a real multi-route experience baseline | Desktop and 390px captures of public, auth, account, audit, report, support, privacy, loading, empty, error, and delayed states produce a ranked system-first defect map; auxiliary vision findings are grounded in images and mechanical accessibility/overflow checks | design-research |
| done | P10 · C12 · D2 | Enforce shared product-experience primitives | Static and component tests measure token, panel, header, button, banner, empty/loading/error-state, tap-target, radius, and hardcoded-color conformance across product routes; the check reports coverage and exceptions rather than claiming subjective perfection | frontend |
| done | P11 · C14 · F7 | Define evidence-backed offer and pricing contracts | Current plans, entitlements, runtime outputs, delivery costs, promises, and observed funnel events map to versioned offer hypotheses; unsupported distinctions fail a parity test and empirical gaps render unknown rather than fabricated demand | product |
| proposed | P3 · C1/C13 · F5 | Make reports explicit projections of canonical intelligence | A fixture-backed projection contract proves report sections, dashboard summaries, and exports derive from one pinned evidence/brief/method bundle and cannot be parsed back into canonical state; old immutable reports remain retrievable | architecture |
| proposed | P12 · C12/C15 · F4/F9 | Measure end-to-end responsiveness and runtime budgets | Reproducible local/preview measurements separate browser rendering, Vercel route time, Supabase round trips, worker queue time, evidence acquisition, inference, and rendering; each budget has a baseline, target, and honest unavailable state | performance |
| done | P12 · C15 · F9 · D7 | Build an authentication and capability preflight matrix | Deterministic checks cover supported login methods, OAuth/env presence without secret disclosure, callback/deletion/support requirements, worker commands, model features, migrations, and failure/recovery states; missing capability blocks before mutation | reliability |
| proposed | P12 · F6/F10 · D8/D9 | Prove portable interfaces and release containment | An architecture contract names every Vercel/Supabase/Hetzner interface, health probe, dependency, migration order, canary, and rollback; a dry-run release fixture fails closed on missing preconditions and emits a machine-readable evidence packet | devops |

## Replenishment rules

- Read the latest ledger rows, `VISION.md`, `FUNDAMENTALS.md`, `USER-STORIES.md`, current canonical implementation packet, and repository test gaps.
- Prefer a different underdeveloped program when the last four completed cards cluster in one area.
- Every idea names a baseline, observed or fixture-backed evidence, an honest-null clause, and a no-production safety boundary.
- Append new ideas; do not rewrite history to make the queue look cleaner.

## Evidence-gap replenishment — 2026-08-07 W006

| Status | Tag | Idea | Baseline / evidence | Expected composition | Capability preflight | Check / honest null / safety | Type |
|---|---|---|---|---|---|---|---|
| done | P6 · F4/F9 · D3/D4/D8 | Prove in-flight inference cancellation containment | ALM-I-010 verified total-deadline and pending-stage cancellation, but its ledger verdict explicitly leaves already-running provider-call cancellation to the per-call timeout boundary | Extend the canonical bounded runtime and telemetry policy; preserve completed stages and existing DeepSeek-only policy; add no second scheduler or provider wrapper | Python, uv, existing runtime budget fixture, and a deterministic blocking fake transport must be present; no provider credential or network source is required | A mock-only test starts an in-flight call, crosses the total deadline, proves bounded return/cancel classification, preserves completed artifacts, records zero live provider calls, and leaves an uncooperative transport as UNKNOWN with a correction tip; no service, customer data, deployment, or live tokens | reliability |

## Evidence-gap replenishment — 2026-08-07 W007

| Status | Tag | Idea | Baseline / evidence | Expected composition | Capability preflight | Check / honest null / safety | Type |
|---|---|---|---|---|---|---|---|
| done | P6/P12 · F2/F4/F9 · D2/D3/D8 | Reconcile intelligence telemetry with authoritative generation-run persistence | ALM-I-010 added an allowlisted `RuntimeTelemetry` sink, while `report_generation_runs` and `intelligence_runs` already persist overlapping runtime fields; repository inspection finds no production use of `telemetry_sink`, so deadline/failure/cache/token/stage semantics can drift between the intelligence runtime and operator records | Add one typed mapping/parity contract at the existing runtime-to-persistence boundary; keep current Supabase tables/RPCs and worker gateway authoritative; do not add a second metrics store, duplicate worker loop, or customer-visible trace surface | Python, uv, current runtime/pipeline/supabase client, static migrations, recording gateway fixtures, and migration checker must be present; no Supabase credential, linked database, service, provider, or network source is required | A mock-only adapter test emits success, timeout, deadline, cancellation-unknown, resumed, and honest-null cases and proves every supported allowlisted field maps once to the existing authoritative run records with normalized status/error vocabulary and no payload leakage; a static contract detects schema/adapter drift; if no safe canonical boundary exists, verdict is parked with the exact missing owner instead of creating a parallel sink; no migration application, service, customer data, deployment, or live tokens | reliability |

## Evidence-gap replenishment — 2026-08-07 W008

| Status | Tag | Idea | Baseline / evidence | Expected composition | Capability preflight | Check / honest null / safety | Type |
|---|---|---|---|---|---|---|---|
| done | P7/P12 · C15 · F2/F9 · D2/D3/D8 | Project authoritative run health into a founder-only recovery view | `report_generation_runs` is private service-role attempt telemetry, `intelligence_runs` is subject-domain run truth, and the web currently reads only minimal intelligence-run identity/status while no founder route consumes report-generation-run health; ALM-I-015 is intended to settle field ownership but does not itself create an operator experience | Add one server-only founder projection at the existing admin boundary after ALM-I-015 is verified; consume the two records according to their declared owners, expose bounded status/age/recovery actions, reuse access primitives, and reveal no raw trace, customer payload, or generic dashboard surface | Node, pnpm, current Supabase generated types, admin access contract, deterministic route/component fixtures, and a verified ALM-I-015 owner matrix must be present; no Supabase credential, browser login, service, or network source is required for the declared check | Fixture-backed server/component tests cover ready/running/delayed/failed/crashed/deadline/cancellation-UNKNOWN/resumed cases, deterministic ordering/age, admin-only access, redaction, and exact correction tips; if ALM-I-015 remains non-verified or a recovery action has no typed owner, verdict is parked rather than inferring state or adding mutation; no linked database, customer data, deployment, service action, or live tokens | frontend/reliability |

## Evidence-gap replenishment — 2026-08-07 W009

| Status | Tag | Idea | Baseline / evidence | Expected composition | Capability preflight | Check / honest null / safety | Type |
|---|---|---|---|---|---|---|---|
| done | P7/P12 · C15 · F2/F3/F9 · D2/D3/D8 | Make founder recovery transitions state-safe and auditable | Repository inspection at `0a7358c` finds `approveAudit`, `requeueAudit`, and `blockAudit` in `web/src/lib/actions/admin.ts` update audits by ID without a server-side current-status predicate, while the corresponding `audit_events` insert is a separate write; `AuditActions` disables some buttons in the client, but client state is not mutation authority and stale/direct submissions can bypass that presentation check | Extend the existing founder admin-action boundary with one typed transition matrix and one authoritative compare-and-transition path that records exactly one matching `audit_events` row; reuse current audit statuses, `requireAdmin`, service-role ownership, and event vocabulary; do not add a second queue, generic workflow engine, silent repair, or customer-facing control | Node, pnpm, current admin action/UI source, generated Supabase types, static migrations and migration checker, recording gateway fixtures, and local SQL contract tests must be present; no Supabase credential, linked database, browser login, service, or production approval is required | A fixture matrix covers approve/requeue/block from every relevant current status plus duplicate/stale submissions: invalid transitions produce zero writes, valid transitions produce one status change and one founder event, actor/note fields are bounded and redacted, and any required RPC is statically checked for fixed `search_path`, service-role/admin authorization, and grants; if atomic event ownership cannot be established from current schema, verdict is parked with the exact missing authority rather than preserving a split write or inventing success; no linked migration, customer data, service action, deployment, or live tokens | security/reliability |

## Evidence-gap replenishment — 2026-08-07 W010

| Status | Tag | Idea | Baseline / evidence | Expected composition | Capability preflight | Check / honest null / safety | Type |
|---|---|---|---|---|---|---|---|
| done | P2/P7/P12 · C7/C8/C15 · F2/F9 · D2/D3/D8 | Drift-test the audit lifecycle vocabulary across producers and projections | Repository inspection finds `audits.status` remains free-form text while web founder actions, worker claim/retry/reaper SQL, customer status projection, report-attempt telemetry, and admin review each encode overlapping status literals; current tests validate local behavior but no artifact proves every producer maps once to an authoritative audit lifecycle state or an explicit UNKNOWN | Add one CI-only lifecycle vocabulary projection and static drift report consumed by tests across existing web, worker, and SQL contracts; keep `audits` and current producer state machines authoritative, preserve the separate report-attempt/intelligence-run vocabularies, and add no runtime registry, schema rewrite, or customer-visible status | Python, Node, pnpm, uv, current web/worker status modules, static migrations, and deterministic fixture extraction must be present; no Supabase credential, linked database, browser, service, provider, or external source is required | A deterministic fixture enumerates every discovered audit status producer/consumer, proves each supported literal has one owner and customer/founder projection, rejects unmapped or conflated values, and renders genuinely ambiguous states UNKNOWN with exact source/correction tips; if source ownership cannot be established statically, return partial/parked rather than inventing a canonical enum; fixtures do not prove live queue behavior, and no migration, customer data, service action, deployment, or live tokens are permitted | reliability/architecture |

## Evidence-gap replenishment — 2026-08-07 W011

| Status | Tag | Idea | Baseline / evidence | Expected composition | Capability preflight | Check / honest null / safety | Type |
|---|---|---|---|---|---|---|---|
| in_flight | P5/P8 · C9/C13 · F7/F8 · D2/D3/D8 | Expose recommendation decisions through the canonical subject workspace | ALM-I-003 verified the service-role recommendation-outcome ledger and the kernel provides `record_decision`, but current web source inspection finds no accept/reject/modify action or owner-scoped decision control—only a demo fixture that renders a prior rejection—so C9 is not yet an operable product contract | Extend the existing subject intelligence workspace with one typed server-action adapter to the authoritative recommendation/decision ledgers and shared experience states; preserve immutable recommendations, owner/admin boundaries, and recurrence semantics; add no client-authoritative state, second decision store, generic dashboard, or efficacy claim | Node, pnpm, current subject/recommendation components, generated Supabase types, static recommendation/decision migrations, recording client fixtures, access-boundary tests, and 390px component checks must be present; no Supabase credential, linked database, browser login, service, provider, or external source is required | Fixture-backed server/component tests cover owner/admin/other-user, accepted/rejected/modified, duplicate/stale/unsupported submissions, loading/error/recovery, 44px controls, and exactly one typed ledger call; invalid or unauthorized cases make zero writes and rejected advice remains suppressed absent new evidence; if `record_decision` and `recommendation_outcomes` ownership cannot be composed without inventing a second state, return partial/parked with the exact missing RPC/authority; fixtures do not prove creator efficacy or retention, and no migration application, customer data, deployment, service action, or live tokens are permitted | product/engineering |

## Evidence-gap replenishment — 2026-08-07 W012

| Status | Tag | Idea | Baseline / evidence | Expected composition | Capability preflight | Check / honest null / safety | Type |
|---|---|---|---|---|---|---|---|
| done | P11 · C14 · F7/F8 · D3/D4/D8 | Make Stripe subscription reconciliation idempotent, ordered, and auditable | Repository inspection at `16fdedd` finds `web/src/app/api/webhooks/stripe/route.ts` never consumes `event.id`, `applySubscription` directly updates `profiles` without checking the update result or appending a provider receipt, `web/src/lib/offer-pricing.ts::planForPriceId` maps unknown price IDs to `free`, and the existing `admin_actions.actor_id` requires a founder profile; there are no Stripe-named v2 tests or commercial-event ledger references, while the existing offer parity suite passes 39 tests and the static migration checker passes 43 files | Compose `Stripe signed event → typed commercial-transition reducer → one service-role-only atomic RPC → append-only idempotency/audit receipt + current profiles entitlement projection → submit_entitled_audit`; keep Stripe authoritative for payment facts, offer-pricing authoritative for supported price mapping, and profiles authoritative for current access; the receipt is history/idempotency evidence, not a second billing store; add no analytics dashboard, client-authoritative plan state, provider fallback, or separate billing engine | Node, pnpm, Python, Stripe SDK, Vitest, generated Supabase types, static migrations, `scripts/check-migrations.py`, sanitized event fixtures, and recording Stripe/Supabase clients must be present; no Stripe/Supabase credential, linked database, browser login, network source, payment action, or production permission is required | Fixture/static tests cover supported active/trialing/cancelled transitions, duplicate/older/equal-time-conflicting events, unknown price IDs, missing profile mapping, failed persistence, and manual-access precedence: valid events produce exactly one receipt plus one atomic profile transition; duplicates/stale/unknown/contradictory cases make zero profile mutations and return an explicit UNKNOWN/BLOCKED correction tip, never `free` by default; if an atomic receipt-plus-profile owner cannot be established, park with the exact RPC/schema gap; assert zero Stripe/Supabase network calls and no migration application, customer data, deployment, payment/service action, or live tokens | commercial/reliability |

## Evidence-gap replenishment — 2026-08-07 W013

| Status | Tag | Idea | Baseline / evidence | Expected composition | Capability preflight | Check / honest null / safety | Type |
|---|---|---|---|---|---|---|---|
| in_flight | P11/P12 · C14/C15 · F7/F9 · D3/D4/D8 | Make checkout intent creation idempotent and recoverable | Repository inspection at `1b6b4b8` finds `web/src/lib/actions/billing.ts::startCheckout` creates a Stripe customer before proving the service-role profile link can persist, ignores that update result, supplies no Stripe idempotency key for customer or checkout-session creation, and has no focused billing/checkout tests; repeated or interrupted submissions can therefore create multiple provider objects while the dashboard only receives a query-string outcome | Compose `authenticated profile + canonical offer → deterministic checkout intent → one reusable customer linkage → one idempotent Stripe session → bounded redirect/recovery state`; reuse the existing billing action, price contract, and service-role profile boundary; do not add a billing store, client plan authority, alternate checkout provider, generic workflow, or extra intake screen | Node, pnpm, Stripe SDK, current auth/billing/offer modules, sanitized profile fixtures, recording Stripe/Supabase clients, and deterministic idempotency-key hashing must be present; no Stripe/Supabase credential, browser login, network source, payment action, linked database, or production permission is required | Fixture tests cover existing-customer reuse, first creation, duplicate/retried submission, profile-link persistence failure, missing price/configuration, plan mismatch, session failure, and redirect recovery: the same profile+plan+offer version emits the same non-secret idempotency key, successful retries make at most one logical customer/session creation, persistence failure stops before a session, and UNKNOWN/BLOCKED carries an exact correction tip rather than simulated success; if the SDK boundary cannot prove replay semantics without a live call, classify provider behavior UNKNOWN while still proving adapter call options; assert zero network/payment/customer mutations and no deployment, linked migration, service action, or live tokens | commercial/reliability |

## Evidence-gap replenishment — 2026-08-07 W014

| Status | Tag | Idea | Baseline / evidence | Expected composition | Capability preflight | Check / honest null / safety | Type |
|---|---|---|---|---|---|---|---|
| proposed | P11/P12 · C14/C15 · F7/F9 · D3/D4/D8 | Make billing-portal session recovery deterministic and bounded | Repository inspection at `697a554` finds `web/src/lib/actions/billing.ts::openBillingPortal` checks only Stripe presence and `stripe_customer_id`, then calls `billingPortal.sessions.create` without focused tests, bounded exception handling, customer-link freshness evidence, or a typed recovery outcome; provider failure can surface as an unclassified server-action error while the dashboard has only query-string billing outcomes | Extend the existing billing action and shared billing recovery vocabulary after W014 checkout intent settles; reuse the canonical Stripe customer link, dashboard outcome surface, offer/access contracts, and recording clients; do not add a billing store, customer-management workflow, alternate provider, generic dashboard, or entitlement mutation | Node, pnpm, Stripe SDK 22.2.0, current auth/billing/dashboard modules, sanitized profile fixtures, recording Stripe clients, and the verified W014 checkout recovery vocabulary must be present; no Stripe/Supabase credential, browser login, network source, customer mutation, linked database, or production approval is required | Fixture tests cover missing/stale customer link, unconfigured provider, successful session, provider exception, missing/unsafe URL, duplicate submission, and bounded return navigation; valid input makes exactly one portal-session call, invalid/failure cases never redirect as success and expose an exact correction tip, while live Stripe customer existence/replay remains UNKNOWN; assert zero network/payment/customer mutations and no deployment, migration, service action, or live tokens | commercial/reliability |

## Evidence-gap replenishment — 2026-08-07 W015

| Status | Tag | Idea | Baseline / evidence | Expected composition | Capability preflight | Check / honest null / safety | Type |
|---|---|---|---|---|---|---|---|
| proposed | P2/P3/P9 · C1/C2/C5/C13 · F5/F8 · D2/D3/D8 | Pin immutable report versions to canonical intelligence provenance | Repository inspection at `c7629ef` finds `finalize_initial_report`/`finalize_refinement_report` persist report path, prompt, template, and bundle versions in `audit_report_versions`, while `intelligence_runs` separately pins subject, Living Brief version, evidence snapshot, methodology, expertise pack, prompt, model configuration, and output schema; no static or runtime contract links an immutable report version to the exact intelligence run, and the MCP projection labels `audits.prompt_version` as `methodology_version` | Extend the existing finalization RPC/report-version record with one optional authoritative intelligence-run reference and a bounded projection manifest; preserve old immutable versions and compatibility reads, keep Supabase ledgers authoritative, and add no report-prose parsing, duplicate provenance store, event platform, or backfilled precision | Python, uv, Node, pnpm, static migrations, current finalization RPCs, intelligence-run schema, worker gateway/ledger fixtures, report route/MCP fixtures, generated types, and migration checker must be present; no Supabase credential, linked database, browser, provider, customer artifact, or network source is required | Static/recording tests prove a new report can pin exactly one same-subject completed intelligence run and project its brief/evidence/methodology/model/schema versions; mismatched, absent, stale, or legacy references render explicit UNKNOWN/correction metadata without blocking retrieval of old reports; no prose is parsed back into canonical state; if generation and intelligence-run ownership cannot be linked from current call sites, return partial/parked with the exact missing owner; no migration application, customer data, deployment, service action, or live tokens | architecture/engineering |
| proposed | P6/P12 · C7/C15 · F4/F9 · D3/D8 | Bound cached peer context before report inference | Repository inspection at `c7629ef` finds `_fetch_benchmark_cache` intentionally fetches the full benchmark dataset and `_format_benchmark_cache` concatenates every deduplicated cached peer with no row, byte, or per-niche cap; the comment estimates about 30 rows, but no deterministic test or telemetry proves prompt growth remains bounded as the table expands | Add one deterministic sort/dedupe/truncation budget at the existing benchmark-cache-to-prompt boundary, composed after peer validity; preserve the single Supabase fetch/cache and DeepSeek-only report path, include explicit omitted/`Data needed` counts, and add no second cache, pagination loop, model summarizer, or provider fallback | Python, uv, current pipeline/core/cache modules, sanitized large recording fixtures, prompt byte counter, existing report/template tests, and the verified peer-validity classification vocabulary must be present; no Supabase credential, linked database, service, provider, browser, or network source is required | Fixture matrices at empty, normal, boundary, and adversarial 1,000-row inputs prove stable ordering, normalized dedupe, per-niche/total row caps, a strict UTF-8 byte ceiling, explicit omitted counts, and no valid high-priority row displaced by invalid input; if a safe product budget is not established, return partial/parked with measured sizes rather than inventing a threshold; assert provider_calls=network_calls=0 and no customer data, migration, deployment, service action, or live tokens | reliability/performance |
