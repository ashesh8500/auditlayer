# v0.1 Freeze — Stable, Quick, Account-Centric — Target 2026-07-16 18:00 UTC

Chat DEFERRED. Scope locked to freeze tomorrow. No new ideas.

> I am operating as grok-4.5 (xAI) per switch note, manual targeted execution, zero delegation.

## 1) What v0.1 Is

Frozen:
- Landing `/`: 4 larger cards + scrollable demo (DONE)
- Auth `/login` stable
- Accounts as source of truth: /accounts grid + /accounts/[id] with tabs Overview | Audits (newest->oldest) | Progression (minimal sparkline from account_progression) | Settings | + /accounts/[id]/settings persistent memory editor
- New audit richer: ?account_id= mode prefills handle/platform/memory, shows connection health (active/refresh/expired), research cache badge (Research from DATE), force_refresh toggle, PDF opt-in checkbox default OFF
- Audit view /audits/[id]: live-timeline + immersive report + share + refinement stable
- Report: deterministic JSON-safe pipeline but Narin-faithful 15-section fidelity, brand ribbon + compact ALM footer
- Worker stable: accounts upsert + progression write + 7d cache + PDF only if requested
- Admin, privacy, support unchanged

Out of v0.1:
- Chat with audit (deferred)
- Full redesign, animations, analytics

## 2) Current Truth (prod)

- Vercel: https://auditlayermedia.com live, workers auditlayer-worker@1 @2 active, queue 0, HERMES_AGENT_ROOT=/opt/auditlayer/.hermes/hermes-agent fixed
- Migrations: 0026 accounts + account_progression, 0027 research cache + force_refresh applied. Prod tables exist, 0 accounts rows, 0 progression (no post-deploy success audit yet)
- Web routes: /app/(app)/ = audits + dashboard + layout only. No /accounts dir. AppHeader: Dashboard | New audit | Admin | Sign out — no Accounts link, inconsistent
- Dashboard: 6 visual dialects: dark #14241f hero, alm-kicker headers, rounded-[var(--radius)] cards, rounded-xl hover -translate, hardcoded border-green-200 bg-green-50 in InstagramConnect, inline style #1877F2, blue callout for limitations, dashed empty state
- Wizard /audits/new: bespoke 3-step with 6 inline SVG icons, animate-in, no account awareness
- Report paths: ReadyReport dark CTA bar 14241f + grid iframe 72vh + ReportViewer + ShareLinks + ImmersiveReport shadow DOM + share /s/[token] — 4 renderers, same CSS not reused
- master-skeleton.html 204 lines: missing .idea-card, .upgrade-box, .powered-by, .score-diagram header, .calendar-grid precise, .timeline-item, .tag pill vs current square; Narin reference worker/templates/narin_reference_template.html 332 lines is canonical Hemal format with full classes
- core.py STANDARD_SECTIONS currently: Executive Summary, Key Metrics, Strengths, Weaknesses, Root Cause Analysis, Peer Comparison, Content Format Analysis, Engagement Growth Strategy, Quick Wins, Success Benchmarks, Audience Profile, Road to, Audit Cadence, Footer, Powered by — WRONG vs Narin approved. Narin approved 15 (skill auditlayer-report-editing): 1 Exec Summary (score diagram + diagnosis callout), 2 Key Metrics (4-card grid + 3-col table Metric|Value|Status with dot green/amber/red + accent callout), 3 Strengths 6 cards sw-grid green left-border, 4 Weaknesses critical first red, 5 Root Cause table + warning callout, 6 Peer Comparison ALWAYS higher followers same niche table Dimension|@handle|@peer|Benchmark + callout, 7 Content Format Analysis format x signal table, 8 Engagement Growth 5 rec-cards numbered circles left 18px top 20px accent-muted 30px 6px radius padding-left 60px, 9 Content Calendar & Creative Board 10 idea-cards across 4 pillars (🎓 Educational 3, 📂 Portfolio 2, 💬 Engagement 3, 🚀 Growth 2) + weekly rhythm table Day|Format|Pillar|Goal (calendar-grid) + how to use callout, 10 Quick Wins day-by-day timeline-items accent dots, 11 Success Benchmarks 3 grouped tables Profile Health/Content Performance/Business Outcomes Metric|Now|30 Days|90 Days|Priority colored text Critical red 600 / Build habit amber / Organic green — targets clean numbers no dots, 12 Audience Profile segment table, 13 Road to Milestone 3-phase timeline + summary table + callout, 14 Audit Cadence Weekly->Monthly->Quarterly->Post-Viral table + callout, 15 Get Execution Plan pricing-aware Standard ($30/mo) vs Extended ($50/mo) check table + encouragement + CTA Upgrade to Extended — $50/month. Footer minimal centered: [ALM badge] AuditLayerMedia @auditlayermedia Follow tips -> with border-top. Brand ribbon teal gradient in-flow at top.

## 3) UI Inconsistency Fixes (Lane A target, 1-3 files each)

Token canonical in globals.css: --bg #f5f1e8, --surface #fffdf8, --text #14241f, --muted #65736e, --line #dcd7ca, --accent #0d9488, --radius 8px, shadows. Use alm-shell (max-w-6xl px), alm-kicker (mono 0.68rem uppercase tracking 0.14em accent), alm-panel (border border-border bg-card shadow-[var(--shadow)]), alm-table-wrap.

Component debt:
- Replace hardcoded bg-green-50 border-green-200 bg-red-50 etc in InstagramConnect + share-links + support with tone variants via Banner: `<Banner tone=var(--green) icon=Check>`. Already exists in live-timeline but not reused.
- Unify cards: single alm-card primitive, remove bg-[#14241f] one-offs except landing hero + login split.
- Single page header: PageHeader component kicker+title+desc, replaces per-page h1 variants.
- EmptyState: size-14 bg-[var(--accent-muted)] icon + title + action button — replaces dashboard dashed + admin No auditsYet text.
- HealthDot: dot green/amber/red + label for connection health, research cache age.
- AppHeader: add Accounts link /accounts with Building2 icon, keep Dashboard as Audit library or Reports, active state via accent underline.

## 4) Accounts as Source of Truth (Lanes B+C)

Migration 0028_account_memory.sql (minimal incremental):
```sql
alter table accounts add column if not exists bio text;
alter table accounts add column if not exists niche text;
alter table accounts add column if not exists goals text;
alter table accounts add column if not exists voice_notes text;
alter table accounts add column if not exists competitors text[] default '{}';
alter table accounts add column if not exists memory jsonb default '{}'::jsonb;
alter table accounts add column if not exists ig_connection_id uuid references instagram_connections(id) on delete set null;
-- RLS backfill: allow users own insert/update/delete
create policy if not exists accounts_insert_own on accounts for insert with check (auth.uid()=user_id);
create policy if not exists accounts_update_own on accounts for update using (auth.uid()=user_id);
create policy if not exists accounts_delete_own on accounts for delete using (auth.uid()=user_id);
```
Note: dashboard show owned accounts limited per plan via PLAN_LIMITS (handled client-side, not DB cap).

Worker backfill on success: pipeline.py _link_account_and_progression already upserts account + writes progression. Extend to set display_name if available from ig_metrics profile, and ig_connection_id if matched.

Frontend:
- /accounts/page.tsx: (supabase as any).from("accounts").select("*, audits:audits(id,status,created_at,report_type,cost_usd) accounting? link"). Order created_at desc. Grid: AccountCard handle @, platform, HealthDot (active if instagram_connections.last_refreshed_at <7d else refresh), last audit date, latest progression followers + delta, quick action New audit href /audits/new?account_id=.

- /accounts/[id]/page.tsx: route param id. Load account, audits where account_id=id order newest first, progression order recorded_at desc. Tabs via ?tab=overview|audits|progression|memory (default overview). Overview shows latest audit lede (from research_snapshot first sentences) + Key Metrics from ig_metrics_snapshot or progression latest. Audits tab scrollable list. Progression tab minimal SVG sparkline (no external deps): map progression followers to 0-100 path, show line + dots.

- /accounts/[id]/settings/page.tsx: form server action updateAccountMemory: fields bio, niche, goals, voice_notes, competitors comma separated -> text[], display_name. Also sections: Connections: show ig_connection matched, status active / needs reactivation if last_refreshed >60d, button re-connect -> /api/auth/instagram/connect?account_id. Research cache controls: last_researched_at, cache_valid_until, Invalidate button -> sets cache_valid_until null via admin client. PDF preference not here (per audit).

Data hints: Wherever real data exists (followers count, engagement, last_refreshed), show HealthDot + numeric with delta from previous progression row.

## 5) Richer New Audit (Lane D)

File set: web/src/app/(app)/audits/new/page.tsx + wizard.tsx + web/src/lib/actions/audits.ts

Behavior:
- ?account_id param: server loads account, if belongs to user prefill handle=account.handle platform=account.platform memory=niche+goals summary.
- UI top: if account_id, show AccountContext chip: avatar placeholder, @handle, platform badge, HealthDot connection health, follower count if progression latest, memory lines niche/goals.
- Second row: if account cache_valid_until > now, Banner accent "Research cached — last refreshed {date 7d ago} from {last_researched_at}". Checkbox Force refresh (writes audits.force_refresh). If not cached, show "Will run fresh sweep (Instagram always fresh)".
- Third: Goal selection unchanged but show contextual hint from account memory.
- Fourth: Optional context textarea prefilled from bio+voice_notes if account memory exists, editable.
- Bottom: PDF opt-in checkbox default OFF label "Include PDF export (adds ~10s, most use HTML only)" writes audits.pdf_requested.
- Submit: audits.ts accepts account_id, force_refresh bool, pdf_requested bool. Upserts account if not exists (service-role fallback for RLS), inserts audit with account_id, force_refresh, pdf_requested.

Migration 0029_pdf_optin.sql:
```sql
alter table audits add column if not exists pdf_requested boolean not null default false;
```

## 6) Report Format Fix — Deterministic but Narin-faithful (Lane E — Critical Path)

Goal: keep safety (nonfinite float rejection, duplicate keys rejection, unsafe HTML rejection, public errors generic) but visual fidelity = Narin hemal.

Steps minimal:

1) Skeleton restore:
Replace worker/auditlayer_worker/templates/master-skeleton.html style with merged canonical from worker/templates/narin_reference_template.html + hermes-profile reference:

Required classes: .container max-w 760px padding 60px 28px 120px, .report-header label 11px uppercase accent, h1 2.1rem 700 letter -0.02, subtitle 0.95 muted, meta flex gap 24px 0.8rem, section margin 48px h2 1.35rem bold border-bottom line, h3 1.05, p 0.95 #44403c, .metric-grid repeat(4,1fr) gap 12, .metric-card bg surface border line radius 10 padding 18 16 center value JetBrains Mono 1.6rem, .value.red/green/amber, label 0.72 uppercase muted, .data-table width 100% collapse 0.88rem th left 600 0.72rem uppercase muted padding 10 14 border-bottom 2px line td 10 14 bottom 1px vertical top first-child 500 num Mono 0.85 right, highlight accent-muted, .tag inline-block 0.72rem 600 padding 2 10 radius 4, strength green-muted green etc, .sw-grid grid 1fr 1fr gap 16 margin 20 0, .sw-card surface border line radius 10 padding 20 22 strength border-left 3px green weakness red sw-label 0.68 700 uppercase tracking 0.1em mb 6, .rec-card surface border line radius 10 padding 22 24 mb 14 relative padding-left 60px .num absolute left 18 top 20 Mono 0.85 600 accent accent-muted 30x30 radius 6 flex center, h4 0.95 600 mb 6 p 0.85 #57534e, .idea-card surface border line radius 10 padding 22 24 mb 14 border-left 3px accent h4 1rem etc meta 0.72 muted uppercase 600, .calendar-grid grid 100px 80px 160px 1fr gap 0 margin 16 0 border line radius 10 overflow hidden .ch 0.68 700 uppercase muted padding 10 14 bg #f5f5f4 border-bottom .cr padding 10 14 border-bottom 0.85, .timeline-item flex gap 16 mb 20 t-dot flex-shrink 0 12x12 radius 50 mt 4 green/red/accent + halo shadow 0 0 0 3px var(--color-muted), .callout bg blue-muted border-left 3px blue padding 16 20 radius 0 8 8 0 margin 20 0 p 0.9 #1e3a5f 500 accent bg accent-muted border-left accent, .upgrade-box gradient 135deg 0d9488 0f766e radius 12 padding 32 margin 32 0 color fff center h3 1.25 etc cta-btn white bg accent text 700 0.9rem padding 12 28 radius 8, .score-diagram surface border line radius 12 padding 24 28 20 mb 48 sd-header flex baseline space-between mb 18 pb 14 border-bottom sd-label 0.68 700 uppercase muted sd-overall Mono 1.8rem 600 sd-row flex center gap 12 mb 8 sd-name width 160 0.8 500 right shrink 0 sd-track flex1 height 8 bg #f0efed radius 4 overflow hidden sd-fill height 100 radius 4 high green mid amber low red sd-num width 28 Mono 0.75 500 muted right, .powered-by flex center gap 8 mt 40 pt 20 border-top line 0.75 muted 500 alm-badge inline-flex center 20x20 bg #1c1917 radius 4 Inter 9px 700 fff, .report-footer mt 60 pt 24 border-top line 0.78 muted. + brand ribbon teal gradient top 100% + responsive @680px container 32 16 80, h1 1.6, metric-grid repeat2 sw-grid 1fr etc + print bg white max-width 100 padding 20 0 avoid breaks.

Placeholders kept: {handle} {platform} {goal} {date} {full_name} {title} {location} {account_type} {milestone} etc.

Section slots replaced via regex as before.

2) Standard sections fix in core.py:
STANDARD_SECTIONS = [
 "Executive Summary",
 "Key Metrics",
 "Strengths",
 "Weaknesses",
 "Root Cause Analysis",
 "Peer Comparison",
 "Content Format Analysis",
 "Engagement Growth Strategy",
 "Content Calendar & Creative Board",
 "Quick Wins — This Week",
 "Success Benchmarks",
 "Audience Profile",
 "Road to [Milestone]",
 "Audit Cadence",
 "Get the Execution Plan",
]
PULSE_SECTIONS = ["Score Breakdown","Key Gaps","Three Immediate Moves"] (keep)
EXTENDED same but with Content Pillars & Ideas etc for now — keep but align first 15 same as standard then extra.
BLUEPRINT keep but ensure 15.

3) Typed renderers in assemble_structured_report_html:

Keep _extract_structured_payload + _structured_text.

Define renderers:

def _render_executive_summary(section, ig): if items contain score? look for value numeric parse -> build score-diagram: overall from first item value or lede extraction, rows from items array: sd-name = item title, sd-track fill width=score %, sd-fill class high >=70 mid >=40 else low, sd-num. Then callout from callout field accent. Wrap in <section><h2> etc.

def _render_key_metrics: metric-grid 4 cards from items[0:4] (Followers, Following, Posts, F/F Ratio) or from ig_metrics_snapshot if connected; then data-table Metric|Value|Status with dot green/amber/red based on lede assessment (item value contains status? use item title/body). After table accent callout.

def _render_strengths/weaknesses: sw-grid div with sw-card strength/weakness each: sw-label Strength N / Weakness N, h4 title, p body.

def _render_root_cause: table Dimension|Reality|Instagram|Gap + warning callout accent? use callout.

def _render_peer_comparison: enforce higher followers rule in prompt, table with peers, callout why same-tier.

def _render_format_analysis: table Format|Signal|Current|Recommended.

def _render_growth: rec-cards numbered circle.

def _render_calendar_board: intro p + h3 pillars + idea-card per item: pillar = part of title? Use items split across 4 pillars hardcoded groups if heading contains Educational etc or distribute 10 ideas across pillars, each idea-card with idea-meta pillar, h4 title, p body, format-tag value. Then calendar-grid Days table: use table field from JSON if provided else default weekly rhythm.

def _render_quick_wins: timeline-item per item: t-dot accent, t-content h4 title, p body.

def _render_benchmarks: 3 grouped tables with Priority column: render each sub-table? If JSON provides table headers include Priority, use colored spans: <span style="color: var(--red); font-weight:600">Critical</span> etc based on text.

def _render_audience: segment table.

def _render_road: timeline-item 3 phases + summary table.

def _render_cadence: cadence table + callout next audit date.

def _render_execution: paragraph Standard vs Extended, comparison table Standard vs Extended checkmarks, personal encouragement, CTA button <a class="cta-button">Upgrade to Extended — $50/month</a> href #.

Implementation: keep single loop over sections but dispatch based on required_heading string to renderer function that returns HTML string using html_lib.escape for safety, no raw HTML from model allowed. Table rendering uses escaped values only.

Keep html_looks_complete / build_section_prompt contract.

4) Prompt tightening in SECTION_SYSTEM_PROMPT + build_section_prompt Output Contract:

Enforce: Return ONE JSON object only, schema {sections:[{heading, lede, items:[{title,body,value?}], table:{headers,rows}, callout?}]} Use every required heading exactly once in order, heading must match character-for-character except Road to [Milestone] uses actual milestone label from audit.milestone_label. heading Road to X starts with Road to . Each lede 1-3 sentences. Max 5 items per section, tables max 12 rows. Plain text values only, no HTML/CSS/URLs as markup. Connected Instagram metrics inserted deterministically locally so analyze but do not invent. Peer Comparison: select peers with HIGHER follower counts than @handle, same niche, real handles only. Do not call tools, do not return HTML.

Also keep benchmarks block formatting.

5) Safety keep: _instagram_metric_block still used only for Key Metrics when connected but now integrates into metric-grid not duplicate.

Verification for Lane E: cd worker && uv run pytest tests/test_template_pipeline.py tests/test_prompt_version.py -q. Also local mock: uv run python -m auditlayer_worker demo --handle test --generator mock and check report has 15 h2 tags grep -c "<h2>" etc no OUTPUT TRUNCATED.

## 7) PDF Opt-in (Lane F)

Already partial: pdf_status columns exist 0025. Add 0029 pdf_requested bool.

pipeline.py:
- After HTML upload, if not audit.pdf_requested: do NOT set pdf_status pending, set pdf_status=null or ready skipped? Keep column nullable or set not pending. In update_audit, if pdf_requested then pdf_status='pending' else pdf_status='skipped' or 'not_requested'.
- Only link account etc always.

pdf_worker.py: claim only where pdf_status='pending' and audits.pdf_requested=true.

web report-viewer.tsx:
- props pdfReady, pdfRequested. If !reportReady show not yet. If reportReady && pdfReady show Download PDF. If reportReady && !pdfReady && pdfRequested show spinner PDF generating... If reportReady && !pdfRequested show button Generate PDF (calls action setPdfRequested true). Action: server action update audit set pdf_requested true, pdf_status pending. Realtime will auto update.

## 8) Freeze Verification (Lane G)

Checklist to freeze v0.1 TOMORROW:

- [ ] npm run typecheck zero errors (use (supabase as any) for new tables to avoid types churn)
- [ ] npm run build passes (web)
- [ ] cd worker && uv run pytest -q 222 passes (or current count)
- [ ] Migrations applied: 0026 0027 0028 0029 on linked project via npx supabase db push
- [ ] Content: /accounts + /accounts/[id] + /accounts/[id]/settings exist, AppHeader links Accounts active
- [ ] New audit richer works with ?account_id
- [ ] Report mock has 15 h2, no OUTPUT TRUNCATED, visual classes present: grep metric-grid, sw-grid, rec-card, idea-card, calendar-grid, timeline-item, score-diagram, callout
- [ ] Prod deploy: vercel --prod + rsync worker + restart @1 @2, journalctl grep connected + hermes_mode inprocess + deepseek-v4-flash, NRestarts 0, queue 0
- [ ] E2E curl: / 200, /login 200, /accounts redirects to login when unauthed but renders when authed (manual browser check)
- [ ] Live test audit submit with @auditlayermedia handle, see account row created, progression row, dashboard Your accounts shows it, /accounts/[id] shows audits newest first + sparkline placeholder + settings shows connection health + cache controls
- [ ] Tag git tag v0.1 + docs/agent-handoff.md update

## 9) Execution Order — Minimal Token Waste

- A + E parallel first (no overlap files): A touches web components, E touches worker templates + core.py
- B after A (needs design primitives)
- C after B (needs accounts routes to add settings)
- D after C (needs account memory columns)
- F after D (pdf optin depends on D migration)
- G after all

Each sub-lane: read 1-3 files max, patch, typecheck/build only own area, no full repo scan. Use (supabase as any) to avoid gen types loop that broke previous.

Risk mit: HERMES_AGENT_ROOT path, ProtectHome fix — keep rsync copy to /opt/auditlayer/.hermes/hermes-agent, chown auditlayer.

Tokens budget: main only verify+deploy.

END
