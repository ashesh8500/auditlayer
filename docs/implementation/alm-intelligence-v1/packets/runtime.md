# Runtime implementation packet

Read `COMMON.md`, locked decisions, ownership, acceptance, contracts, `docs/report-runtime-slo.md`, and worker AGENTS. Implement the intelligence runtime in owned worker paths.

Deliver normalized evidence packets, bounded website collection with SSRF/redirect/size/deadline protections, deterministic context projection, single-channel fast path, concurrent multi-channel analysis (maximum three), bounded synthesis, exact cache keys, durable stage resume, evidence-ID validation, deterministic score inputs, recommendation/change outputs, and telemetry. Integrate behind compatibility boundaries so existing report types and security gates continue to pass.

Production inference remains DeepSeek V4 Flash, tool-free, no delegation, no memory, no fallback. Do not edit migrations or web. Do not call live models in tests, deploy, push, or mutate production. Write a checkpoint JSON and commit the worktree branch.
