# Model policy

- Product report generation is pinned to provider `deepseek` and model `deepseek-v4-flash`.
- Product runtime must not inherit the model or provider from a coding, reviewer, or personal profile.
- No Codex fallback is permitted for product report generation.
- Interactive profiles may change model only through an explicit, session-scoped founder action.
